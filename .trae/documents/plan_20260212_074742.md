我将为您编写全面、规范的接口文档和数据库文档。

### 文档生成计划

我将分两步完成文档的编写，最终将所有文档保存在 `d:\Yisu\Yisu-Hotel-backend\docs\pc\` 目录下。

#### 第一步：生成数据库架构文档 (`Database_Schema.md`)
我将分析 `src/models/entities/` 目录下的所有实体文件，生成一份包含所有数据表的详细文档。
*   **内容结构**：
    *   **表名与描述**：每个表的英文名及中文用途说明。
    *   **字段详情表**：包含字段名称、数据类型、约束条件（主键、非空、唯一等）、默认值、外键关系。
    *   **索引信息**：列出表上定义的索引。

#### 第二步：生成 API 接口文档
我将根据路由模块，分别生成以下四个接口文档：
1.  **认证模块** (`Auth_API.md`)：涵盖注册、登录、验证码、密码重置等接口。
2.  **用户模块** (`User_API.md`)：涵盖个人资料获取与更新、消息列表等接口。
3.  **酒店模块** (`Hotel_API.md`)：涵盖酒店的增删改查、审核状态查询等接口。
4.  **管理模块** (`Admin_API.md`)：涵盖管理员的酒店审核列表、批量审核等接口。

*   **文档格式**：每个接口将包含以下标准信息：
    *   **接口名称**：清晰描述接口功能。
    *   **请求信息**：URL 路径、HTTP 方法、鉴权要求（Header Authorization）。
    *   **请求参数**：详细列出 Query 或 Body 参数，包括字段名、类型、是否必填、说明及校验规则（如长度限制、枚举值）。
    *   **响应结构**：成功的 JSON 响应示例，包含 `code`, `msg`, `data` 结构。
    *   **错误码**：列出该接口可能返回的特定业务错误码及其含义。

### 目录结构预览
最终生成的文档目录结构如下：
```text
docs/pc/
├── Database_Schema.md  (数据库结构说明)
├── Auth_API.md         (认证相关接口)
├── User_API.md         (用户相关接口)
├── Hotel_API.md        (酒店相关接口)
└── Admin_API.md        (后台管理接口)
```
